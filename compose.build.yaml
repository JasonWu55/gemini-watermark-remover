services:
  web:
    image: gemini-watermark-remover-web:local
    build:
      context: .
      target: web

  api:
    image: gemini-watermark-remover-api:local
    build:
      context: .
      target: api

  cli:
    image: gemini-watermark-remover-cli:local
    build:
      context: .
      target: cli
